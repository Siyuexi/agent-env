{{- if .Values.apiPriority.enabled }}
apiVersion: flowcontrol.apiserver.k8s.io/v1
kind: FlowSchema
metadata:
  name: {{ include "arl-operator.fullname" . }}
  labels:
    {{- include "arl-operator.labels" . | nindent 4 }}
spec:
  priorityLevelConfiguration:
    name: {{ .Values.apiPriority.priorityLevel | default "workload-high" }}
  matchingPrecedence: {{ .Values.apiPriority.matchingPrecedence | default 900 }}
  rules:
    - subjects:
        - kind: ServiceAccount
          serviceAccount:
            name: {{ include "arl-operator.serviceAccountName" . }}
            namespace: {{ .Release.Namespace }}
        {{- if .Values.gateway.enabled }}
        - kind: ServiceAccount
          serviceAccount:
            name: {{ include "arl-operator.fullname" . }}-gateway
            namespace: {{ .Release.Namespace }}
        {{- end }}
      resourceRules:
        - apiGroups: ["*"]
          resources: ["*"]
          verbs: ["*"]
          namespaces: ["*"]
{{- end }}
